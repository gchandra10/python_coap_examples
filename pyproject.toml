[tool.poetry]
name = "python-coap-examples"
version = "0.1.0"
description = ""
authors = ["Ganesh Chandrasekaran <gchandra@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
aiocoap = "^0.4.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
